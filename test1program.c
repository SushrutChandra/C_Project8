#include <stdio.h>

/* Test program with too few comments */

int main(void) {
  int idx, factorial, n;

  scanf("%d", &n);
  factorial= 1;
  idx= 1;

  while (id <= n)
    factorial *= idx++;

  printf("%d\n", factorial);

  return 0;
}
