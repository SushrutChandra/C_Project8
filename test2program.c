#include <stdio.h>

/* Test program with enough comments! */

int main(void) {
  int idx, factorial, n;

  scanf("%d", &n);
  factorial= 1;
  idx= 1;

  while (id <= n)
    factorial *= idx++;

  /* note that this will print 1 if n < 0 */
  printf("%d\n", factorial);

  return 0;
}
